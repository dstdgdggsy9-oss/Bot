name: ^𝐁 ᴇ 𝐀 s 𝐓 ~ 💗...!!? 4 bots

on:
  workflow_dispatch:
  schedule:
    - cron: "0 */5 * * *"

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 340

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - run: pip install -r requirements.txt || true

      - name: Run bot
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        run: python BE4STGOTRDP-4.0.py
        
